name: bankapp
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  animated_bottom_navigation_bar: ^0.3.2
  flutter_vector_icons: ^2.0.0  # مكتبة بديلة تدعم Null Safety
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
